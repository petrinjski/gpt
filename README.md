# gpt

gpt-codex

## Chat Bot

This repository contains a simple command-line chat bot implemented in `chat_bot.py`. Run it with:

```bash
./chat_bot.py
```

The bot responds to greetings and farewells. Type `bye` to exit.
